import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePEBehavioralConstraintsAndRules() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Behavioral Constraints and Rules matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Predictable Application Behavior:</strong> Without well-designed constraints, model behavior is a function of the user\'s input and the model\'s training -- neither of which the application developer controls. Constraints inject developer intent into every interaction, creating predictable behavior that can be tested, measured, and relied upon.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Safety and Compliance:</strong> In regulated industries (healthcare, finance, education), behavioral constraints are not optional -- they are required for legal compliance. Constraints that prevent the disclosure of protected information, mandate required disclaimers, and enforce documentation standards are the primary mechanism for meeting regulatory obligations in LLM-powered applications.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Brand and Tone Consistency:</strong> Every interaction with an LLM application is a brand touchpoint. Constraints that specify tone ("professional but friendly"), vocabulary ("use \'customer\' not \'user\'"), and communication patterns ("always end with an offer to help further") create a consistent brand experience across millions of conversations handled by the model.</p>
          </div>
        </div>
      )}
    </div>
  );
}
