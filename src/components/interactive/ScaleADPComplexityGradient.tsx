import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScaleADPComplexityGradient() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Complexity Gradient matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Complexity Has Compound Costs:</strong> Each rung up the ladder does not just add one cost --- it multiplies across several dimensions simultaneously. An agent is not just "more tokens." It is more tokens AND more latency AND more failure modes AND more debugging time AND more monitoring infrastructure AND more testing surface area.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Premature Complexity Is the Root of Most Agent Failures:</strong> When teams report that "agents don\'t work," the root cause is almost always that they skipped rungs. They went directly from "we need an LLM feature" to "let\'s build a multi-agent system." The agent then fails on tasks that a prompt chain could have handled, and the team concludes that LLM applications are unreliable.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>The Gradient Enables Incremental Investment:</strong> Starting simple means you can ship a working v1 fast (often in days), learn from real usage, and invest in complexity where the data tells you to. This is faster and cheaper than building a complex system, discovering it fails in unexpected ways, and trying to figure out which of the 15 moving parts is responsible.</p>
          </div>
        </div>
      )}
    </div>
  );
}
