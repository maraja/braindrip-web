import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScaleAACSemanticMemory() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Semantic Memory matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Supplements LLM Parametric Knowledge:</strong> LLMs encode general knowledge in their weights, but this knowledge is static (frozen at training cutoff), probabilistic (sometimes wrong), and non-specific (does not include private or domain-specific facts). Semantic memory provides a complementary knowledge layer that is updatable, verifiable, and customizable to the specific user and domain.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Enables Consistent Factual Recall:</strong> Without explicit semantic memory, agents may give inconsistent answers about the same fact across different sessions or even within the same conversation. A semantic memory store provides a single source of truth: "The project uses PostgreSQL 15" is stored once and retrieved consistently every time it is relevant.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Supports Domain-Specific Expertise:</strong> A general-purpose LLM may have limited knowledge about a specific company\'s products, internal tools, or domain terminology. Semantic memory allows the agent to accumulate domain-specific knowledge that makes it increasingly expert in the user\'s particular context.</p>
          </div>
        </div>
      )}
    </div>
  );
}
