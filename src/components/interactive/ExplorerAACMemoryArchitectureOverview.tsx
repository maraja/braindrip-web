import { useState } from 'react';

const DETAILS = [
    { label: 'Working memory effective limit', detail: '~20-40K tokens for reliable information use, even in models with 100K+ context windows, due to attention dilution in the \"lost in the middle\" phenomenon' },
    { label: 'Long-term memory retrieval latency', detail: 'Vector store queries take 10-100ms; database queries take 1-50ms. This is fast enough for interactive use but adds up over many retrievals per agent step' },
    { label: 'Embedding dimensions', detail: 'Common embedding models produce 768-3072 dimensional vectors. Higher dimensions capture more semantic nuance but increase storage and retrieval costs' },
    { label: 'Memory consolidation trigger', detail: 'Common strategies include consolidating at end-of-conversation, at regular intervals (every N messages), or when working memory exceeds a threshold' },
    { label: 'Retrieval count', detail: 'Loading 3-5 relevant memory chunks per agent step is typical; more risks overwhelming working memory, fewer risks missing critical context' },
    { label: 'Storage formats', detail: 'Conversation memories are typically stored as (text, embedding, metadata) tuples where metadata includes timestamp, topic, importance score, and source' },
];

export default function ExplorerAACMemoryArchitectureOverview() {
  const [open, setOpen] = useState<number | null>(null);

  return (
    <div style={{ fontFamily: "'Source Sans 3', system-ui, sans-serif", background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: '14px', padding: '2rem', margin: '2.5rem 0' }}>
      <div style={{ marginBottom: '1.5rem' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.5rem' }}>
          <span style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: '22px', height: '22px', borderRadius: '6px', background: 'rgba(139, 168, 136, 0.15)', fontSize: '12px' }}>&#9654;</span>
          <span style={{ fontSize: '10px', fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.12em', color: '#6E8B6B' }}>Interactive</span>
        </div>
        <h3 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.3rem', fontWeight: 600, color: '#2C3E2D', margin: 0 }}>
          Memory Architecture Overview — Key Details Explorer
        </h3>
        <p style={{ fontSize: '0.88rem', color: '#5A6B5C', margin: '0.4rem 0 0 0', lineHeight: 1.6 }}>
          Click each card to explore the technical details of memory architecture overview.
        </p>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem' }}>
        {DETAILS.map((d, i) => (
          <button key={i} onClick={() => setOpen(open === i ? null : i)} style={{
            textAlign: 'left' as const, background: open === i ? '#F0EBE1' : '#FDFBF7', border: '1px solid #E5DFD3',
            borderRadius: '10px', padding: '0.875rem 1rem', cursor: 'pointer', width: '100%', transition: 'background 0.2s ease',
          }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
              <span style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '0.95rem', fontWeight: 600, color: '#2C3E2D' }}>
                {d.label}
              </span>
              <span style={{ fontSize: '0.8rem', color: '#7A8B7C', transform: open === i ? 'rotate(90deg)' : 'none', transition: 'transform 0.2s ease' }}>
                &#9654;
              </span>
            </div>
            {open === i && (
              <p style={{ fontSize: '0.85rem', color: '#5A6B5C', lineHeight: 1.6, margin: '0.5rem 0 0 0' }}>
                {d.detail}
              </p>
            )}
          </button>
        ))}
      </div>
    </div>
  );
}
