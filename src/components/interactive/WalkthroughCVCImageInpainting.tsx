import { useState } from 'react';

const STEPS = [
    { title: '1. Problem Formulation', desc: 'Given an image I and a binary mask M (1 where pixels are missing), inpainting produces a completed image &#123;I&#125; such that: &#123;I&#125;  (1 - M) = I  (1 - M) (known pixels are preserved) &#123;I&#125;  M looks natural and consistent with the surrounding context' },
    { title: '2. Early Deep Learning Approaches', desc: 'Context Encoders (Pathak et al., 2016) trained an encoder-decoder with adversarial loss to fill in central 64x64 rectangular holes. The architecture resembles an autoencoder where the encoder sees the masked image and the decoder predicts the missing region:  [equation]  with _&#123;rec&#125; = 0.' },
    { title: '3. Partial Convolutions', desc: '(2018) introduced partial convolutions to handle irregular masks. Standard convolutions treat missing pixels as valid, corrupting features.' },
    { title: '4. Gated Convolutions', desc: '(2019) argued that hard binary masking is suboptimal and proposed learning soft attention masks via gated convolutions:  [equation] [equation] [equation]  The gating mechanism learns where and how much each spatial location should contribute, providing a more flexible alternative to partial.' },
    { title: '5. Contextual Attention', desc: '(2018) introduced a contextual attention layer that explicitly borrows features from known regions to fill unknown regions:  Extract patches from known regions as a collection of reference features.' },
    { title: '6. Two-Stage Coarse-to-Fine Architecture', desc: 'Many methods (Yu et al., 2018; Zeng et al., 2019) use a two-stage approach:  Coarse network: Produces a rough initial completion using dilated convolutions for large receptive fields. Refinement network: Uses contextual attention or non-local mechanisms to refine details.' },
];

export default function WalkthroughCVCImageInpainting() {
  const [step, setStep] = useState(0);
  const current = STEPS[step];

  return (
    <div style={{ fontFamily: "'Source Sans 3', system-ui, sans-serif", background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: '14px', padding: '2rem', margin: '2.5rem 0' }}>
      <div style={{ marginBottom: '1.5rem' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.5rem' }}>
          <span style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: '22px', height: '22px', borderRadius: '6px', background: 'rgba(139, 168, 136, 0.15)', fontSize: '12px' }}>&#9654;</span>
          <span style={{ fontSize: '10px', fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.12em', color: '#6E8B6B' }}>Interactive Walkthrough</span>
        </div>
        <h3 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.3rem', fontWeight: 600, color: '#2C3E2D', margin: 0 }}>
          Image Inpainting \u2014 Step by Step
        </h3>
        <p style={{ fontSize: '0.88rem', color: '#5A6B5C', margin: '0.4rem 0 0 0', lineHeight: 1.6 }}>
          Walk through how image inpainting works, one stage at a time.
        </p>
      </div>

      <div style={{ display: 'flex', gap: '0.35rem', marginBottom: '1.25rem' }}>
        {STEPS.map((_, i) => (
          <button key={i} onClick={() => setStep(i)} style={{
            flex: 1, height: '4px', borderRadius: '2px',
            background: i <= step ? '#C76B4A' : '#E5DFD3',
            border: 'none', cursor: 'pointer', transition: 'background 0.2s ease',
          }} />
        ))}
      </div>

      <div style={{ marginBottom: '1.25rem' }}>
        <h4 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.05rem', fontWeight: 600, color: '#2C3E2D', margin: '0 0 0.4rem 0' }}>
          {current.title}
        </h4>
        <p style={{ fontSize: '0.85rem', color: '#5A6B5C', lineHeight: 1.6, margin: 0 }}>
          {current.desc}
        </p>
      </div>

      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <button onClick={() => setStep(Math.max(0, step - 1))} disabled={step === 0} style={{
          padding: '0.4rem 1rem', borderRadius: '6px', border: '1px solid #E5DFD3',
          background: step === 0 ? '#F5F0E8' : '#FDFBF7', color: step === 0 ? '#B0A898' : '#5A6B5C',
          fontSize: '0.8rem', cursor: step === 0 ? 'default' : 'pointer', fontFamily: "'Source Sans 3', system-ui, sans-serif",
        }}>
          &#8592; Previous
        </button>
        <span style={{ fontSize: '0.75rem', color: '#7A8B7C', fontFamily: "'JetBrains Mono', monospace" }}>
          {step + 1} / {STEPS.length}
        </span>
        <button onClick={() => setStep(Math.min(STEPS.length - 1, step + 1))} disabled={step === STEPS.length - 1} style={{
          padding: '0.4rem 1rem', borderRadius: '6px',
          border: `1px solid ${step === STEPS.length - 1 ? '#E5DFD3' : '#C76B4A'}`,
          background: step === STEPS.length - 1 ? '#F5F0E8' : 'rgba(199, 107, 74, 0.08)',
          color: step === STEPS.length - 1 ? '#B0A898' : '#C76B4A',
          fontSize: '0.8rem', fontWeight: 500, cursor: step === STEPS.length - 1 ? 'default' : 'pointer',
          fontFamily: "'Source Sans 3', system-ui, sans-serif",
        }}>
          Next &#8594;
        </button>
      </div>
    </div>
  );
}
