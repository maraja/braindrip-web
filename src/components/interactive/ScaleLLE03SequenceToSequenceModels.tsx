import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScaleLLE03SequenceToSequenceModels() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Sequence-to-Sequence Models matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Establishing the Encoder-Decoder Paradigm:</strong> Seq2Seq established a pattern that would echo through the entire history of LLMs: encode an input into a representation, then decode that representation into an output. This same paradigm reappears in the Transformer\'s encoder-decoder architecture (01-attention-is-all-you-need.md), in T5\'s text-to-text framework (05-t5-text-to-text-framework.md), and conceptually in every prompt-to-response interaction with a modern LLM.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Proving End-to-End Learning Could Work:</strong> Before Seq2Seq, complex NLP tasks required pipelines of specialized components. Seq2Seq demonstrated that a single neural network trained end-to-end could learn the entire mapping from input to output without handcrafted features.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Revealing the Fixed-Length Bottleneck:</strong> The encoder\'s final hidden state had to compress an entire sentence (or paragraph) into a single vector. For short sentences, this worked well.</p>
          </div>
        </div>
      )}
    </div>
  );
}
