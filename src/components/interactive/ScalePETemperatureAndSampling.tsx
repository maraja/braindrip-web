import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePETemperatureAndSampling() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Temperature and Sampling matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Task-Specific Calibration:</strong> Different tasks have optimal temperature ranges, and miscalibration produces measurable quality loss:  Using T=1.0 for JSON extraction can produce malformed output 10-30% of the time that would be well-formed at T=0.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Reproducibility and Testing:</strong> At T=0, the same prompt produces the same output (with the same model version), enabling deterministic testing. This is critical for prompt evaluation: you cannot A/B test prompt variants if sampling variance exceeds the signal from prompt changes.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Cost Implications of Sampling:</strong> Temperature does not directly affect cost (you pay per token regardless), but indirectly, lower temperature produces shorter, more focused outputs (fewer "wandering" tokens), while higher temperature can produce longer, more exploratory responses. For high-volume applications, T=0 outputs are typically 10-20% shorter than T=1.0 outputs for the same prompt, translating to real output token savings.</p>
          </div>
        </div>
      )}
    </div>
  );
}
