import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePEClassificationAndExtractionAtScale() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Classification and Extraction at Scale matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Small Error Rates Scale to Large Numbers:</strong> A 2% error rate is acceptable for 100 inputs (2 errors) but produces 20,000 errors at 1 million inputs. At scale, even minor systematic biases — such as consistently misclassifying a specific subtype — compound into significant data quality problems that affect downstream decision-making.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Consistency Is Harder Than Accuracy:</strong> A model that is 95% accurate but inconsistent (sometimes classifying the same input differently) is less useful for analytics than one that is 90% accurate but perfectly consistent. Analytics and trend detection depend on consistent application of classification criteria over time.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Prompt Degradation Is Invisible Without Monitoring:</strong> Unlike traditional software that fails loudly with errors, prompt degradation fails silently with slightly worse outputs. Without active monitoring, a prompt that was 92% accurate at deployment can drift to 78% accuracy over months as data distributions shift — and no one notices until the downstream impact becomes visible.</p>
          </div>
        </div>
      )}
    </div>
  );
}
