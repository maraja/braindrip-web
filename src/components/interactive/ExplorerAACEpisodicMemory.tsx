import { useState } from 'react';

const DETAILS = [
    { label: 'Episode granularity', detail: 'Store one episode per task or subtask, not per individual action. Individual actions are too fine-grained for useful retrieval; entire conversation sessions are too coarse. A single self-contained task attempt is the right unit' },
    { label: 'Storage overhead', detail: 'A typical episode is 200-500 tokens of text plus a 1536-dimensional embedding vector. Storing 10,000 episodes requires approximately 15MB of vector storage and 2-5MB of text' },
    { label: 'Retrieval count', detail: 'Loading 2-5 relevant episodes per new task is typical. More episodes increase context cost; fewer may miss relevant experiences' },
    { label: 'Recency bias', detail: 'Recent episodes are typically weighted higher (1.5-2x) than older episodes in retrieval scoring, on the assumption that recent experiences are more relevant to current conditions' },
    { label: 'Success/failure ratio', detail: 'Aim to retrieve a mix of successful and failed episodes. Successful episodes provide templates; failed episodes provide warnings. A 3:1 success-to-failure ratio is a reasonable default' },
    { label: 'Episode lifetime', detail: 'Not all episodes are valuable forever. Periodic review and pruning (removing episodes about deprecated tools, outdated APIs, or irrelevant contexts) maintains store quality' },
];

export default function ExplorerAACEpisodicMemory() {
  const [open, setOpen] = useState<number | null>(null);

  return (
    <div style={{ fontFamily: "'Source Sans 3', system-ui, sans-serif", background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: '14px', padding: '2rem', margin: '2.5rem 0' }}>
      <div style={{ marginBottom: '1.5rem' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.5rem' }}>
          <span style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: '22px', height: '22px', borderRadius: '6px', background: 'rgba(139, 168, 136, 0.15)', fontSize: '12px' }}>&#9654;</span>
          <span style={{ fontSize: '10px', fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.12em', color: '#6E8B6B' }}>Interactive</span>
        </div>
        <h3 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.3rem', fontWeight: 600, color: '#2C3E2D', margin: 0 }}>
          Episodic Memory \u2014 Key Details Explorer
        </h3>
        <p style={{ fontSize: '0.88rem', color: '#5A6B5C', margin: '0.4rem 0 0 0', lineHeight: 1.6 }}>
          Click each card to explore the technical details.
        </p>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem' }}>
        {DETAILS.map((d, i) => (
          <button key={i} onClick={() => setOpen(open === i ? null : i)} style={{
            textAlign: 'left' as const, background: open === i ? '#F0EBE1' : '#FDFBF7', border: '1px solid #E5DFD3',
            borderRadius: '10px', padding: '0.875rem 1rem', cursor: 'pointer', width: '100%', transition: 'background 0.2s ease',
          }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
              <span style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '0.95rem', fontWeight: 600, color: '#2C3E2D' }}>
                {d.label}
              </span>
              <span style={{ fontSize: '0.8rem', color: '#7A8B7C', transform: open === i ? 'rotate(90deg)' : 'none', transition: 'transform 0.2s ease' }}>
                &#9654;
              </span>
            </div>
            {open === i && (
              <p style={{ fontSize: '0.85rem', color: '#5A6B5C', lineHeight: 1.6, margin: '0.5rem 0 0 0' }}>
                {d.detail}
              </p>
            )}
          </button>
        ))}
      </div>
    </div>
  );
}
