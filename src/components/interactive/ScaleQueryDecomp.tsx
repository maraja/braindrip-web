import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: 'system-ui, sans-serif' };
export default function ScaleQueryDecomp() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>⚡ REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>Query decomposition breaks complex questions into simpler sub-questions that can each be answered independently. How much does this help with multi-hop reasoning?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact →
        </button>
      ) : (
        <div style={{ padding: '0.75rem 1rem', background: '#C76B4A' + '0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
          <p style={{ fontSize: '0.9rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0, fontWeight: 500 }}>Query decomposition improves multi-hop QA accuracy by 15–30% on benchmarks like MuSiQue and HotpotQA. For example, "Which country has the larger GDP, the birthplace of Einstein or the country that hosted the 2024 Olympics?" decomposes into three retrievable sub-questions. Without decomposition, RAG retrieves irrelevant documents 60%+ of the time on such complex queries. With decomposition, each sub-query is simple enough for precise retrieval.</p>
        </div>
      )}
    </div>
  );
}
