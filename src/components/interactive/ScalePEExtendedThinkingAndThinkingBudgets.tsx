import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePEExtendedThinkingAndThinkingBudgets() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Extended Thinking and Thinking Budgets matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Formalized Inference-Time Compute Scaling:</strong> Extended thinking formalizes the insight from self-consistency, tree-of-thought, and other techniques that spending more compute at inference time can improve output quality. But instead of requiring external orchestration (multiple API calls, voting, tree search), extended thinking internalizes the additional computation within a single model call.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Controllable Quality-Cost Trade-off:</strong> By making the thinking budget an explicit parameter, extended thinking gives practitioners fine-grained control over the quality-cost trade-off. A coding task might warrant 10,000 thinking tokens; a simple formatting task might warrant none.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Shifting the Prompt Engineering Landscape:</strong> Extended thinking changes what prompt engineering needs to accomplish. With thinking-enabled models, the prompt engineer\'s job shifts from eliciting reasoning (the model will reason internally regardless) to providing the right context, constraints, and goals.</p>
          </div>
        </div>
      )}
    </div>
  );
}
