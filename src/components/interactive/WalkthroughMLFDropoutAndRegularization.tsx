import { useState } from 'react';

const STEPS = [
    { title: '1. The Dropout Mechanism', desc: 'During training, for each neuron with activation h_i in a layer, dropout applies a binary mask:  [equation]  where p is the dropout rate (probability of being dropped). At each training step, a new random mask is sampled, so the network effectively trains a different sub-architecture every step.' },
    { title: '2. Inverted Dropout', desc: 'The standard implementation uses inverted dropout, which scales activations during training rather than at test time:  [equation]  The scaling by &#123;1&#125;&#123;1-p&#125; ensures that the expected value of &#123;h&#125;_i equals h_i:  [equation]  This means no modification is needed at test time -- the model can be used.' },
    { title: '3. Dropout as Implicit Ensemble', desc: 'A network with n neurons that can each be dropped has 2^n possible sub-networks. Dropout trains all of them simultaneously (with shared weights).' },
    { title: '4. Dropout as Approximate Bayesian Inference', desc: 'Gal and Ghahramani (2016) proved that a network with dropout applied before every weight layer is mathematically equivalent to an approximation to a deep Gaussian process. Specifically, dropout training minimizes the KL divergence between an approximate posterior over weights and the true posterior.' },
    { title: '5. Monte Carlo Dropout for Uncertainty', desc: 'Because dropout has a Bayesian interpretation, we can obtain uncertainty estimates by leaving dropout on at test time and running multiple forward passes:  [equation] [equation]  where each m_t is a different dropout mask.' },
    { title: '6. Typical Dropout Rates', desc: 'Input layers: p = 0.1 to 0.2 (dropping input features is aggressive). Hidden layers: p = 0.5 is the classic default (Hinton\'s original recommendation).' },
];

export default function WalkthroughMLFDropoutAndRegularization() {
  const [step, setStep] = useState(0);
  const current = STEPS[step];

  return (
    <div style={{ fontFamily: "'Source Sans 3', system-ui, sans-serif", background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: '14px', padding: '2rem', margin: '2.5rem 0' }}>
      <div style={{ marginBottom: '1.5rem' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.5rem' }}>
          <span style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: '22px', height: '22px', borderRadius: '6px', background: 'rgba(139, 168, 136, 0.15)', fontSize: '12px' }}>&#9654;</span>
          <span style={{ fontSize: '10px', fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.12em', color: '#6E8B6B' }}>Interactive Walkthrough</span>
        </div>
        <h3 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.3rem', fontWeight: 600, color: '#2C3E2D', margin: 0 }}>
          Dropout and Regularization \u2014 Step by Step
        </h3>
        <p style={{ fontSize: '0.88rem', color: '#5A6B5C', margin: '0.4rem 0 0 0', lineHeight: 1.6 }}>
          Walk through how dropout and regularization works, one stage at a time.
        </p>
      </div>

      <div style={{ display: 'flex', gap: '0.35rem', marginBottom: '1.25rem' }}>
        {STEPS.map((_, i) => (
          <button key={i} onClick={() => setStep(i)} style={{
            flex: 1, height: '4px', borderRadius: '2px',
            background: i <= step ? '#C76B4A' : '#E5DFD3',
            border: 'none', cursor: 'pointer', transition: 'background 0.2s ease',
          }} />
        ))}
      </div>

      <div style={{ marginBottom: '1.25rem' }}>
        <h4 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.05rem', fontWeight: 600, color: '#2C3E2D', margin: '0 0 0.4rem 0' }}>
          {current.title}
        </h4>
        <p style={{ fontSize: '0.85rem', color: '#5A6B5C', lineHeight: 1.6, margin: 0 }}>
          {current.desc}
        </p>
      </div>

      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <button onClick={() => setStep(Math.max(0, step - 1))} disabled={step === 0} style={{
          padding: '0.4rem 1rem', borderRadius: '6px', border: '1px solid #E5DFD3',
          background: step === 0 ? '#F5F0E8' : '#FDFBF7', color: step === 0 ? '#B0A898' : '#5A6B5C',
          fontSize: '0.8rem', cursor: step === 0 ? 'default' : 'pointer', fontFamily: "'Source Sans 3', system-ui, sans-serif",
        }}>
          &#8592; Previous
        </button>
        <span style={{ fontSize: '0.75rem', color: '#7A8B7C', fontFamily: "'JetBrains Mono', monospace" }}>
          {step + 1} / {STEPS.length}
        </span>
        <button onClick={() => setStep(Math.min(STEPS.length - 1, step + 1))} disabled={step === STEPS.length - 1} style={{
          padding: '0.4rem 1rem', borderRadius: '6px',
          border: `1px solid ${step === STEPS.length - 1 ? '#E5DFD3' : '#C76B4A'}`,
          background: step === STEPS.length - 1 ? '#F5F0E8' : 'rgba(199, 107, 74, 0.08)',
          color: step === STEPS.length - 1 ? '#B0A898' : '#C76B4A',
          fontSize: '0.8rem', fontWeight: 500, cursor: step === STEPS.length - 1 ? 'default' : 'pointer',
          fontFamily: "'Source Sans 3', system-ui, sans-serif",
        }}>
          Next &#8594;
        </button>
      </div>
    </div>
  );
}
