import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePECodeReviewAndDebuggingPrompts() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Code Review and Debugging Prompts matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Bug Finding Is Harder Than Bug Creating:</strong> Models are generally better at generating code than finding bugs in existing code, because generation is a forward process while debugging requires backward reasoning from symptoms to causes. This means debugging prompts must be more carefully structured than generation prompts to achieve comparable quality.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Systematic Review Catches More Issues:</strong> Human code reviewers suffer from anchoring bias (focusing on the first issue found) and fatigue (attention degrades after 200-400 lines of review). Model-based review can systematically check every specified category across the entire codebase without fatigue.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Security Vulnerabilities Require Specialized Prompts:</strong> Generic code review prompts miss 60-70% of security vulnerabilities because the model defaults to checking functionality and style. Security-specific prompts with explicit vulnerability checklists (OWASP Top 10, CWE categories) improve vulnerability detection from 30-40% to 60-75%.</p>
          </div>
        </div>
      )}
    </div>
  );
}
