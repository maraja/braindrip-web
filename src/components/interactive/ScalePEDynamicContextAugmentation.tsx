import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePEDynamicContextAugmentation() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Dynamic Context Augmentation matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Complex Questions Require Multiple Perspectives:</strong> Single-round retrieval works for simple factual lookups ("What is the capital of France?") but fails for questions requiring synthesis, comparison, or multi-step reasoning. Research shows that 30-40% of real-world information-seeking questions benefit from iterative retrieval, and complex analytical questions benefit from 2-4 retrieval rounds.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Efficiency Through Targeted Retrieval:</strong> Paradoxically, dynamic retrieval can be more efficient than aggressive upfront retrieval. Instead of retrieving 20 chunks and hoping the right ones are included, dynamic retrieval starts with 5 well-targeted chunks, assesses coverage, and fetches only the additional 2-3 chunks needed to fill specific gaps.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Enabling Agentic RAG:</strong> Dynamic context augmentation is the foundation of agentic RAG systems where the model actively drives the retrieval process rather than passively consuming pre-retrieved context. This paradigm shift — from model-as-consumer to model-as-researcher — enables more sophisticated information-seeking behaviors that mirror human research processes.</p>
          </div>
        </div>
      )}
    </div>
  );
}
