import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePEPromptDebuggingAndFailureAnalysis() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Prompt Debugging and Failure Analysis matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Reducing Mean Time to Resolution:</strong> Without a systematic debugging workflow, teams spend hours or days chasing symptoms — tweaking wording randomly, adding more instructions (often making the prompt worse), or concluding the model "just cannot do this." A disciplined reproduce-isolate-fix-verify workflow reduces mean time to resolution from hours to 30-60 minutes for most failures. The key time saver is the isolation step: once you know which component is responsible, the fix is usually straightforward.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Building Institutional Knowledge:</strong> Every debugged failure contributes to the team\'s understanding of how the model behaves and what patterns cause problems. Over time, this institutional knowledge accelerates future debugging and improves first-draft prompt quality.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Continuous Improvement Loop:</strong> Debugging is the critical link between detecting failures and preventing them. The complete improvement loop is: monitoring detects failures, debugging diagnoses root causes, fixes are applied and verified, and the failing inputs are added to the eval suite as regression cases.</p>
          </div>
        </div>
      )}
    </div>
  );
}
