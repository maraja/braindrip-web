import { useState } from 'react';
const baseStyle = { background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: 14, padding: '1.25rem', margin: '1.5rem 0', fontFamily: "system-ui, sans-serif" };
export default function ScalePEDelimiterAndMarkupStrategies() {
  const [revealed, setRevealed] = useState(false);
  return (
    <div style={baseStyle}>
      <p style={{ fontSize: '0.8rem', fontWeight: 700, color: '#2C3E2D', marginBottom: 10, letterSpacing: '0.05em' }}>\u26A1 REAL-WORLD IMPACT</p>
      <p style={{ fontSize: '0.9rem', color: '#3D4F3E', lineHeight: 1.5, marginBottom: 12 }}>How does Delimiter and Markup Strategies matter in practice?</p>
      {!revealed ? (
        <button onClick={() => setRevealed(true)} style={{ padding: '6px 16px', borderRadius: 20, border: '1px solid #C76B4A', background: 'transparent', color: '#C76B4A', fontSize: '0.8rem', cursor: 'pointer', fontWeight: 600 }}>
          Reveal Impact \u2192
        </button>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '0.75rem' }}>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Measurable Quality Improvement:</strong> Studies and production evaluations show:  Prompts with clear delimiters achieve 15-20% higher section adherence (the model respects section boundaries) compared to undelimited prompts. XML-tagged prompts reduce content contamination (model confusing context with instructions) by 25-35%.</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Reliable Programmatic Parsing:</strong> When the model\'s output needs to be parsed by downstream code, asking the model to wrap output in delimiters enables reliable extraction:  This produces output that can be parsed with simple string operations or XML parsers, avoiding fragile regex-based extraction from unstructured text. Parse failure rates drop from 5-10% (unstructured) to &lt;1% (delimited).</p>
          </div>
          <div style={{ padding: '0.75rem 1rem', background: '#C76B4A0C', borderRadius: 10, borderLeft: '3px solid #C76B4A' }}>
            <p style={{ fontSize: '0.82rem', color: '#2C3E2D', lineHeight: 1.5, margin: 0 }}><strong>Prompt Injection Defense:</strong> Delimiters are a key defense against prompt injection. By wrapping user input in explicit tags and instructing the model to treat tagged content as data:  The model is more likely to treat &lt;user_input&gt; content as data rather than instructions, reducing injection success rates.</p>
          </div>
        </div>
      )}
    </div>
  );
}
