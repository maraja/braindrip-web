import { useState } from 'react';

const DETAILS = [
    { label: 'Template slot count', detail: 'Most production dynamic prompts use 4-8 modular slots. More than 10 slots increases assembly complexity without proportional benefit.' },
    { label: 'Assembly latency', detail: 'Template assembly adds 1-5ms to request processing, negligible compared to LLM inference time.' },
    { label: 'Token budget management', detail: 'Dynamic assembly must track total token count to ensure the assembled prompt stays within budget (10-20% of context window). Blocks should have maximum token limits.' },
    { label: 'Cache invalidation', detail: 'Dynamic prompts may reduce API-level prompt caching effectiveness since different users receive different prompts. Providers with prefix caching (Anthropic) benefit when the static portion of the template is at the beginning.' },
    { label: 'A/B testing sample size', detail: 'Reliable prompt A/B tests require at least 500-1,000 conversations per variant to achieve statistical significance on most metrics.' },
    { label: 'Rollback time', detail: 'Feature-flag-based rollback is effectively instant (the next request uses the old prompt), while code-deployed prompt changes require a full deployment cycle (minutes to hours).' },
];

export default function ExplorerPEDynamicSystemPrompts() {
  const [open, setOpen] = useState<number | null>(null);

  return (
    <div style={{ fontFamily: "'Source Sans 3', system-ui, sans-serif", background: '#FDFBF7', border: '1px solid #E5DFD3', borderRadius: '14px', padding: '2rem', margin: '2.5rem 0' }}>
      <div style={{ marginBottom: '1.5rem' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.5rem' }}>
          <span style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: '22px', height: '22px', borderRadius: '6px', background: 'rgba(139, 168, 136, 0.15)', fontSize: '12px' }}>&#9654;</span>
          <span style={{ fontSize: '10px', fontWeight: 700, textTransform: 'uppercase' as const, letterSpacing: '0.12em', color: '#6E8B6B' }}>Interactive</span>
        </div>
        <h3 style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '1.3rem', fontWeight: 600, color: '#2C3E2D', margin: 0 }}>
          Dynamic System Prompts \u2014 Key Details Explorer
        </h3>
        <p style={{ fontSize: '0.88rem', color: '#5A6B5C', margin: '0.4rem 0 0 0', lineHeight: 1.6 }}>
          Click each card to explore the technical details.
        </p>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem' }}>
        {DETAILS.map((d, i) => (
          <button key={i} onClick={() => setOpen(open === i ? null : i)} style={{
            textAlign: 'left' as const, background: open === i ? '#F0EBE1' : '#FDFBF7', border: '1px solid #E5DFD3',
            borderRadius: '10px', padding: '0.875rem 1rem', cursor: 'pointer', width: '100%', transition: 'background 0.2s ease',
          }}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
              <span style={{ fontFamily: "'Source Serif 4', Georgia, serif", fontSize: '0.95rem', fontWeight: 600, color: '#2C3E2D' }}>
                {d.label}
              </span>
              <span style={{ fontSize: '0.8rem', color: '#7A8B7C', transform: open === i ? 'rotate(90deg)' : 'none', transition: 'transform 0.2s ease' }}>
                &#9654;
              </span>
            </div>
            {open === i && (
              <p style={{ fontSize: '0.85rem', color: '#5A6B5C', lineHeight: 1.6, margin: '0.5rem 0 0 0' }}>
                {d.detail}
              </p>
            )}
          </button>
        ))}
      </div>
    </div>
  );
}
